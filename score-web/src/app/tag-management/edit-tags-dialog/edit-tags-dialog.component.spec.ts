import {EditTagsDialogComponent} from './edit-tags-dialog.component';

describe('EditTagsDialogComponent', () => {
  it('should be defined', () => {
    expect(EditTagsDialogComponent).toBeTruthy();
  });
});
