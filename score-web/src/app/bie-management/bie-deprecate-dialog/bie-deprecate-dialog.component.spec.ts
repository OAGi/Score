import {BieDeprecateDialogComponent} from './bie-deprecate-dialog.component';

describe('BieDeprecateDialogComponent', () => {
  it('should be defined', () => {
    expect(BieDeprecateDialogComponent).toBeTruthy();
  });
});
