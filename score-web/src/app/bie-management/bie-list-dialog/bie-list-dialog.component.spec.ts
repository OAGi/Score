import {BieListDialogComponent} from './bie-list-dialog.component';

describe('BieListDialogComponent', () => {
  it('should be defined', () => {
    expect(BieListDialogComponent).toBeTruthy();
  });
});
