import {MetaHeaderDialogComponent} from './meta-header-dialog.component';

describe('MetaHeaderDialogComponent', () => {
  it('should be defined', () => {
    expect(MetaHeaderDialogComponent).toBeTruthy();
  });
});
