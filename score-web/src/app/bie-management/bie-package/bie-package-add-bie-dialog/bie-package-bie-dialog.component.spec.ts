import {BiePackageBieDialogComponent} from './bie-package-bie-dialog.component';

describe('BiePackageBieDialogComponent', () => {
  it('should be defined', () => {
    expect(BiePackageBieDialogComponent).toBeTruthy();
  });
});
