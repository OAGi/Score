import {BiePackageDetailComponent} from './bie-package-detail.component';

describe('BiePackageDetailComponent', () => {
  it('should be defined', () => {
    expect(BiePackageDetailComponent).toBeTruthy();
  });
});
