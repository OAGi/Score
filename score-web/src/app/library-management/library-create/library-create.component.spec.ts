import {LibraryCreateComponent} from './library-create.component';

describe('LibraryCreateComponent', () => {
  it('should be defined', () => {
    expect(LibraryCreateComponent).toBeTruthy();
  });
});
