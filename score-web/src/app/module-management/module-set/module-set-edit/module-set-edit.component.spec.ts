import {ModuleSetEditComponent} from './module-set-edit.component';

describe('ModuleSetEditComponent', () => {
  it('should be defined', () => {
    expect(ModuleSetEditComponent).toBeTruthy();
  });
});
