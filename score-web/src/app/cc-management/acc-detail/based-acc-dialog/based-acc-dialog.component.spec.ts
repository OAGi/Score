import {BasedAccDialogComponent} from './based-acc-dialog.component';

describe('BasedAccDialogComponent', () => {
  it('should be defined', () => {
    expect(BasedAccDialogComponent).toBeTruthy();
  });
});
