import {AppendAssociationDialogComponent} from './append-association-dialog.component';

describe('AppendAssociationDialogComponent', () => {
  it('should be defined', () => {
    expect(AppendAssociationDialogComponent).toBeTruthy();
  });
});
