import {CreateVerbDialogComponent} from './create-verb-dialog.component';

describe('CreateVerbDialogComponent', () => {
  it('should be defined', () => {
    expect(CreateVerbDialogComponent).toBeTruthy();
  });
});
