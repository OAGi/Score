import {SearchOptionsDialogComponent} from './search-options-dialog.component';

describe('SearchOptionsDialogComponent', () => {
  it('should be defined', () => {
    expect(SearchOptionsDialogComponent).toBeTruthy();
  });
});
