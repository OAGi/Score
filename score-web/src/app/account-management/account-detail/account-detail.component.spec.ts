import {AccountDetailComponent} from './account-detail.component';

describe('AccountDetailComponent', () => {
  it('should be defined', () => {
    expect(AccountDetailComponent).toBeTruthy();
  });
});
