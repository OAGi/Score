import {AccountCreateComponent} from './account-create.component';

describe('AccountCreateComponent', () => {
  it('should be defined', () => {
    expect(AccountCreateComponent).toBeTruthy();
  });
});
