import {TransferOwnershipListComponent} from './transfer-ownership-list.component';

describe('TransferOwnershipListComponent', () => {
  it('should be defined', () => {
    expect(TransferOwnershipListComponent).toBeTruthy();
  });
});
