import {AccountListDialogComponent} from './account-list-dialog.component';

describe('AccountListDialogComponent', () => {
  it('should be defined', () => {
    expect(AccountListDialogComponent).toBeTruthy();
  });
});
