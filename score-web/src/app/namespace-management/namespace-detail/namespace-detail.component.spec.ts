import {NamespaceDetailComponent} from './namespace-detail.component';

describe('NamespaceDetailComponent', () => {
  it('should be defined', () => {
    expect(NamespaceDetailComponent).toBeTruthy();
  });
});
