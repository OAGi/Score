import {SettingsAccountComponent} from './settings-account.component';

describe('SettingsAccountComponent', () => {
  it('should be defined', () => {
    expect(SettingsAccountComponent).toBeTruthy();
  });
});
