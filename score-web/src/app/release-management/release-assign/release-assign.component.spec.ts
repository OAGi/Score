import {ReleaseAssignComponent} from './release-assign.component';

describe('ReleaseAssignComponent', () => {
  it('should be defined', () => {
    expect(ReleaseAssignComponent).toBeTruthy();
  });
});
