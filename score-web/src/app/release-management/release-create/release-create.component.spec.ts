import {ReleaseCreateComponent} from './release-create.component';

describe('ReleaseCreateComponent', () => {
  it('should be defined', () => {
    expect(ReleaseCreateComponent).toBeTruthy();
  });
});
