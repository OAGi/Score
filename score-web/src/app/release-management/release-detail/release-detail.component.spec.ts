import {ReleaseDetailComponent} from './release-detail.component';

describe('ReleaseDetailComponent', () => {
  it('should be defined', () => {
    expect(ReleaseDetailComponent).toBeTruthy();
  });
});
