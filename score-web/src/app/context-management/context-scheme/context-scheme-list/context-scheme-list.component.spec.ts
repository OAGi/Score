import {ContextSchemeListComponent} from './context-scheme-list.component';

describe('ContextSchemeListComponent', () => {
  it('should be defined', () => {
    expect(ContextSchemeListComponent).toBeTruthy();
  });
});
