import {BusinessContextDetailComponent} from './business-context-detail.component';

describe('BusinessContextDetailComponent', () => {
  it('should be defined', () => {
    expect(BusinessContextDetailComponent).toBeTruthy();
  });
});
