import {BusinessContextCreateComponent} from './business-context-create.component';

describe('BusinessContextCreateComponent', () => {
  it('should be defined', () => {
    expect(BusinessContextCreateComponent).toBeTruthy();
  });
});
