import {BusinessContextListComponent} from './business-context-list.component';

describe('BusinessContextListComponent', () => {
  it('should be defined', () => {
    expect(BusinessContextListComponent).toBeTruthy();
  });
});
