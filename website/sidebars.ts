import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// The documentation sidebar is generated from the docs/ folder structure.
// Ordering and category labels are controlled by numeric folder prefixes and
// each folder's _category_.json file.
const sidebars: SidebarsConfig = {
  docsSidebar: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
